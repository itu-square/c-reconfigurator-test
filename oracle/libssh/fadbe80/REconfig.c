#include <assert.h>
typedef int feature;
extern feature _reconfig_WITH_SSH1;
extern feature _reconfig__WIN32;
extern feature _reconfig_WITH_SERVER;
extern feature _reconfig__MSC_VER;
void* _reconfig_skip () { assert(0); return 0; }